services:
  # Flask app service
  web:
    build: .
    container_name: hopes-dreams-web
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - shared-network
networks:
  shared-network:
    external: true